# Power House Gym — Desktop App dependencies

# Web API (Vercel)
flask==3.0.0
flask-cors==4.0.0

# GUI
customtkinter==5.2.2

# Database / ORM
sqlalchemy==2.0.25
psycopg2-binary==2.9.9   # optional: only used if PostgreSQL is available
                          # (the app falls back to a local SQLite file otherwise)

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
PyJWT==2.8.0

# Config
python-dotenv==1.0.0

# Build (.exe)
pyinstaller==6.3.0
